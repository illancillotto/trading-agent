version: '3.8'

services:
  app:
    build: .
    container_name: trading-agent
    restart: unless-stopped
    ports:
      - "5611:5611"
    volumes:
      - ./data:/app/data
    environment:
      - PYTHONUNBUFFERED=1
